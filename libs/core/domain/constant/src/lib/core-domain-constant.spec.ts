import { coreDomainConstant } from './core-domain-constant';

describe('coreDomainConstant', () => {
  it('should work', () => {
    expect(coreDomainConstant()).toEqual('core-domain-constant');
  });
});
